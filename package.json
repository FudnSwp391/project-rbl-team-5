{
  "name": "techcycle",
  "version": "1.0.0",
  "description": "TechCycle - Electronics Marketplace & Repair Platform",
  "scripts": {
    "install:all": "npm install --legacy-peer-deps && npm install --prefix backend --legacy-peer-deps && npm install --prefix frontend --legacy-peer-deps && npm install --prefix chatbot-server --legacy-peer-deps",
    "server": "npm start --prefix backend",
    "client": "npm run dev --prefix frontend",
    "chatbot": "npm start --prefix chatbot-server",
    "dev": "concurrently \"npm run server\" \"npm run client\" \"npm run chatbot\"",
    "start": "npm run build --prefix frontend && npm start --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^10.0.3"
  }
}
